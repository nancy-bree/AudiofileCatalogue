<div class="nav navbar-nav col-md-6">
                @using (Html.BeginForm("SearchResults", "Audiofile", FormMethod.Get, new { role = "search", @class = "navbar-form" }))
                {
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="Найти файл" name="searchString" id="searchString">
                    <div class="input-group-btn">
                        <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                    </div>
                </div>
                }
            </div>